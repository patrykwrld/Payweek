<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ (API 31) reads this instead of android:allowBackup.

    Nothing leaves the device by either route. Cloud backup would put the
    signed-in session and the cached shift list in Google Drive; device
    transfer would carry them to a new phone without a fresh sign-in. Both
    are unnecessary - the data is in Supabase - and both would widen where a
    person's pay history exists.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
