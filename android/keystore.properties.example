# Copy to android/keystore.properties and fill in. NEVER commit the real
# file or the .jks — losing or leaking the upload key means you can no
# longer publish updates to this listing.
storeFile=payweek-upload.jks
storePassword=
keyAlias=payweek-upload
keyPassword=
